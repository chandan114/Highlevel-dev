// To Uniquely Identify the Name of Each worker from config
export enum KafkaWorkerEnum {
  TEST_KAFKA_WORKER = 'testing_kafka_worker',
}

// To Uniquely Identify the Name of Each Consumer
export enum KafkaConsumerGroupIdEnum {
  TEST_KAFKA_WORKER = 'testing_kafka_worker_group_id',
}

// To Uniquely Use this Enums to Either Consume an Event or Produce an Event
export enum KafkaTopicEnums {
  TEST_KAFKA_WORKER = 'testing_kafka_worker_topic_id',
}
